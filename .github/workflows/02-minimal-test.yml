name: 02 Minimal Test

on:
  issue_comment:
    types: [created]

permissions:
  issues: write
  contents: write

jobs:
  test:
    if: contains(github.event.comment.body, '@claude')
    runs-on: ubuntu-latest
    steps:
      - run: echo '02-minimal-triggered''
